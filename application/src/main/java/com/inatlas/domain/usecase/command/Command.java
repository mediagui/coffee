/**
 * Java interface representing a command. (Command Pattern)
 * Implementing classes must define the execute method.
 */
package com.inatlas.domain.usecase.command;

public interface Command {
  void execute();
}
